# Estia Player

A simple static music player with two playlists:

- **Fun** — Usher, Kanye West, Drake
- **Focus** — house mixes for working

Playback is via embedded YouTube, so it's just static HTML/CSS/JS with no
backend or API keys needed. Open `index.html` in a browser to run it.
